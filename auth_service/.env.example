PORT=8082
HOST=0.0.0.0

# Keycloak server base URL (no trailing slash)
KEYCLOAK_URL=http://localhost:8080

# Shared client credentials — create client "pos-auth-service" in each tenant realm
KEYCLOAK_CLIENT_ID=pos-auth-service
KEYCLOAK_CLIENT_SECRET=REPLACE_ME
